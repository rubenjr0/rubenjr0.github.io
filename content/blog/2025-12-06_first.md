+++
title = "First Post"
description = "Nothing to see here... yet."
+++

Oh hi!

```rs
type Option(T) { None, Some(T) }

inc(Option(U8)) -> Option(U8);
inc(None) = None;
inc(Some(x)) = Some(x + 1);

main { 
    x = Option.Some(1);
    y = inc(x);
}
```
